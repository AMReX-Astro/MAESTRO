#PBS -A ast006
#PBS -N process
#PBS -j oe
#PBS -q batch
#PBS -l walltime=01:30:00,size=4

cd $PBS_O_WORKDIR

PID=$!
trap 'kill -s TERM $PID' EXIT TERM HUP XCPU KILL

# run the process script 
./process.jaguar


rm -f process.pid



