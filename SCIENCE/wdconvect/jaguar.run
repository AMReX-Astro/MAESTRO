#PBS -A ast006
#PBS -N wdconvect
#PBS -j oe
#PBS -q batch
#PBS -l walltime=02:30:00,size=512

cd $PBS_O_WORKDIR

# run the compression script to tar up the plot and checkpoint files
# as they are created.
$HOME/fParallel/scripts/compressor &
PID=$!
trap 'kill -s TERM $PID' EXIT TERM HUP XCPU KILL


aprun -n 512 ./main.Linux.xt4.mpi.exe inputs_3d 

rm -f compressor.pid

