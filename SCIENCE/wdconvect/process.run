#PBS -A ast006
#PBS -N process
#PBS -j oe
#PBS -q batch
#PBS -l walltime=00:30:00,size=4

cd $PBS_O_WORKDIR

# run the compression script to tar up the plot and checkpoint files
# as they are created.
./process_and_compress 

PID=$!
trap 'kill -s TERM $PID' EXIT TERM HUP XCPU KILL

rm -f compressor.pid



